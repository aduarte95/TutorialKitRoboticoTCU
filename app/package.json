{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . tutorial-robokit --overwrite --platform=win32 --arch=x64 --prune=true --out=release-builds --electronVersion=1.4.13"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.0.2",
    "electron-packager": "^13.0.1"
  },
  "dependencies": {
    "bootstrap": "^4.2.1",
    "electron-reload": "^1.4.0"
  }
}
